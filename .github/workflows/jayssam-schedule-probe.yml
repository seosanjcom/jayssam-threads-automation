name: Jayssam Schedule Probe

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Report trigger
        run: |
          echo "event=$GITHUB_EVENT_NAME"
          echo "sha=$GITHUB_SHA"
